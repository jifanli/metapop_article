# profiles starting at l8, using li23, but considering more appropriate ranges.
# For those that already have a suitable range in profile8, we did not rerun them, but instead copied the results from profile8.